apiVersion: v1
kind: Service
metadata:
  name: {{ include "sm3-migration.fullname" . }}
  labels:
    {{- include "sm3-migration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sm3-migration.selectorLabels" . | nindent 4 }}
